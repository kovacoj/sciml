#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$ROOT/env.sh"

echo "=== Building OpenFOAM applications ==="

wmake "$ROOT/applications/torchFoamSmoke"
wmake "$ROOT/applications/fvNeuralLHO"

echo "=== Build complete ==="
