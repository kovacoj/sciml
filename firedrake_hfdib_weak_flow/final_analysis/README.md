# Validated Stokes Preconditioning Package

This additive package uses only validated coefficient and five-seed MLP results.
Run `python3 build_validated_stokes_package.py` to regenerate SVG figures and CSV tables.

Headline result: exact correction velocity error `0.00607` versus oracle `0.00523`
at `64x32`, a ratio of `1.161`.
